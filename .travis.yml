language: node_js
node_js:
  - "10"

before_install:
  - npm i -g tsd@0.6.5

install:
  - cd v5-unity
  - npm install
  - tsd install

script:
  - npm run production-build

deploy:
  provider: releases
  api_key: "$GITHUB_OAUTH_TOKEN"
  file: build/index.bundle.js
  skip_cleanup: true